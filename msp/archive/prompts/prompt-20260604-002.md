# 任务 20260604-002

## 角色
你是 Writer（代码修改者）。你只修改代码，不运行测试，不验证结果。

## 项目约束
- 纯前端 HTML/CSS/JS 游戏，单文件结构
- 使用 window.Zhan 命名空间
- data.js 禁止写 function（polyfill 除外）
- core.js 禁止碰 DOM
- ui.js 禁止直接修改 state
- 所有修改必须先备份到指定目录

## 当前任务
把 zhan_v1.99_sprint4.html 从单文件拆成四个文件：index.html + data.js + core.js + ui.js。纯机械拆分，不修改任何逻辑。

## 修改范围
zhan_v1.99_sprint4.html

## 具体约束
- data.js: 只包含 CONFIG / BOSSES / RELICS / CARD_TYPES / BUFF_TYPES 纯数据声明，禁止写 function（IIFE 除外）
- core.js: 只包含游戏逻辑（Zhan.Systems / Zhan.Rules / Zhan.Engine / Zhan.Test + 所有全局函数 + 启动脚本），禁止 document. / DOM
- ui.js: 只包含渲染和DOM事件（Zhan.UI 全部方法），禁止直接修改 state
- index.html: HTML骨架 + 内联CSS + script src=data.js core.js ui.js 按顺序引入
- 关键：三个 JS 文件内不能有 </script> 标签残留！提取 script 块内容时必须去掉 <script> 和 </script> 标签本身
- 不修改原始文件，在 code/ 目录下产出四个新文件，UTF-8 with BOM 编码
- CSS全部留在index.html的style标签里

## 验证要求
- data.js function关键字<=2（只允许IIFE和字符串字面量）
- 三个JS文件括号全部闭合（{和}数量相等）
- 所有JS文件不包含</script>
- index.html只有3个script src标签，无内联script块
- 浏览器打开index.html无console错误，游戏正常

## 文件位置
C:\Users\kyzha\.openclaw\workspace\projects\zhan\zhan_v1.99_sprint4.html

## 工作目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan

## 备份目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan\msp\archive\backup-20260604-002

## 输出要求（必须逐项完成，缺一不可）
1. 列出你修改的每个文件的**完整绝对路径**
2. 对每个修改的文件，输出 **git diff 格式的变更**：
   - 用 Markdown diff 代码块包裹（三个反引号 + diff 语言标记）
   - 格式示例（缩进表示代码块内容）：
       --- a/file.js
       +++ b/file.js
       @@ -10,5 +10,7 @@
       -   old code
       +   new code
   - 即使项目没有 git 仓库，也要模拟输出 diff 风格的变更说明
   - 必须包含行号范围（@@ -旧起始,旧行数 +新起始,新行数 @@）
3. 列出备份文件路径
4. 逐条说明你认为每条验证要求是否满足（**仅供参考，最终判定由 Scheduler/Verifier 审阅**）
5. 最后输出一行：TASK_DONE
