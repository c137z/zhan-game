# 任务 20240603-020

## 角色
你是 Writer（代码修改者）。你只修改代码，不运行测试，不验证结果。

## 项目约束
- 纯前端 HTML/CSS/JS 游戏，单文件结构
- 使用 window.Zhan 命名空间
- data.js 禁止写 function（polyfill 除外）
- core.js 禁止碰 DOM
- ui.js 禁止直接修改 state
- 所有修改必须先备份到指定目录

## 当前任务
修复 boss_first trait 的死声明。当前 maine_coon 的 traits 中 boss_first 声明了 events: ['TURN_START'] 和 params: {}，但 Zhan.Systems.Boss._traitHandlers 中没有 boss_first 的 handler（实际逻辑在 executeTurn 中通过硬编码 if(boss.id==='maine_coon') 实现）。需要统一：要么在 _traitHandlers 中添加 boss_first handler 并移除 executeTurn 中的硬编码，要么将 boss_first 的 events 改为空数组表示此 trait 不走声明式系统。

## 修改范围
zhan_v1.99_sprint4.html

## 具体约束
- 只改 Zhan.Systems.Boss._traitHandlers + BOSSES.maine_coon.traits + executeTurn 中 maine_coon 相关硬编码
- 方案：移除 boss_first 的 events: ['TURN_START'] 声明（改为 events: []），因为它的逻辑在 executeTurn 开头通过 'if (G.boss && G.boss.id === "maine_coon")' 硬编码处理
- 在 boss_first trait 上加注释说明：'// 此 trait 在 executeTurn 开头硬编码处理（Boss先手），不走 processEvent'
- 不改其他任何逻辑

## 验证要求
- maine_coon boss 的 boss_first trait 不再有无效的 events 声明
- maine_coon 先手逻辑不变（executeTurn 开始的硬编码检查仍然生效）
- processEvent 遍历 maine_coon traits 时不会为 boss_first 尝试匹配 handler（因为 events=[]）

## 文件位置
C:\Users\kyzha\.openclaw\workspace\projects\zhan\zhan_v1.99_sprint4.html

## 工作目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan

## 备份目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan\msp\archive\backup-20240603-020

## 输出要求（必须逐项完成，缺一不可）
1. 列出你修改的每个文件的**完整绝对路径**
2. 对每个修改的文件，输出 **git diff 格式的变更**：
   - 用 Markdown diff 代码块包裹（三个反引号 + diff 语言标记）
   - 格式示例（缩进表示代码块内容）：
       --- a/file.js
       +++ b/file.js
       @@ -10,5 +10,7 @@
       -   old code
       +   new code
   - 即使项目没有 git 仓库，也要模拟输出 diff 风格的变更说明
   - 必须包含行号范围（@@ -旧起始,旧行数 +新起始,新行数 @@）
3. 列出备份文件路径
4. 逐条说明你认为每条验证要求是否满足（**仅供参考，最终判定由 Scheduler/Verifier 审阅**）
5. 最后输出一行：TASK_DONE
