# 任务 20240603-003

## 角色
你是 Writer（代码修改者）。你只修改代码，不运行测试，不验证结果。

## 项目约束
- 纯前端 HTML/CSS/JS 游戏，单文件结构
- 使用 window.Zhan 命名空间
- data.js 禁止写 function（polyfill 除外）
- core.js 禁止碰 DOM
- ui.js 禁止直接修改 state
- 所有修改必须先备份到指定目录

## 当前任务
修复三个 P0 bug：1）executeTurn末尾异步渲染问题——在setTimeout回调和_maineCoonFirst分支末尾都显式调用Zhan.UI.render(G)；2）pullCard中万能核心占位多push null——重构为只在第一个非锁定槽位放一个null占位；3）renderSlot中万能核心硬编码i===0——改为动态查找第一个非锁定槽位。

## 修改范围
zhan_v1.99_sprint4.html

## 具体约束
- 保持现有 Zhan 命名空间结构
- 不改 data.js
- 不改 CSS 类名
- 所有修改先备份

## 验证要求
- 结束回合后 UI 立即刷新
- slot[0]被锁定时万能核心显示在 slot[1]
- pullCard 后 slot.length 等于实际牌数+占位null数

## 文件位置
C:\Users\kyzha\.openclaw\workspace\projects\zhan_v1.99_sprint4.html

## 备份目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan\msp\archive\backup-20240603-003

## 输出要求
1. 修改完成后，列出修改的文件路径
2. 如果有 git diff，输出 diff
3. 备份文件路径
4. 最后输出一行：TASK_DONE
