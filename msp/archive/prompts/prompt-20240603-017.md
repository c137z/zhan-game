# 任务 20240603-017

## 角色
你是 Writer（代码修改者）。你只修改代码，不运行测试，不验证结果。

## 项目约束
- 纯前端 HTML/CSS/JS 游戏，单文件结构
- 使用 window.Zhan 命名空间
- data.js 禁止写 function（polyfill 除外）
- core.js 禁止碰 DOM
- ui.js 禁止直接修改 state
- 所有修改必须先备份到指定目录

## 当前任务
修复 computeCombos 中万能牌在 minCombo 不满足时被浪费的 bug。当前逻辑：在检查 comboLen >= minCombo 之前就把万能牌标记为 claimed。导致短连击（如 [attack, wild]，minCombo=3）中万能牌被消耗但既不算攻击/防御也不扣血。修复：将 claimed 标记移到 minCombo 判定内部。

## 修改范围
zhan_v1.99_sprint4.html

## 具体约束
- 只改 Zhan.Rules.computeCombos 函数
- 将 'for (var ci = i; ci < j; ci++) { ... claimed = true ... }' 这整段移到 'if (comboLen >= minCombo) {' 内部
- 保证 minCombo 不满足时 wild card 不被 claimed，正常进入后续的未消除惩罚计算
- 不改其他任何逻辑

## 验证要求
- slot=[attack, wild], minCombo=3 → 万能牌不被 claimed，计入未消除惩罚（扣血）
- slot=[attack, attack, wild], minCombo=3 → 万能牌被 claimed，连击 n=3，正常结算
- slot=[attack, attack, attack], minCombo=3 → 三个 attack，无万能牌，结果不变
- slot=[wild, wild, attack], minCombo=3 → 两个 wild 都被 claimed 加入 attack 连击，n=3

## 文件位置
C:\Users\kyzha\.openclaw\workspace\projects\zhan\zhan_v1.99_sprint4.html

## 工作目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan

## 备份目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan\msp\archive\backup-20240603-017

## 输出要求（必须逐项完成，缺一不可）
1. 列出你修改的每个文件的**完整绝对路径**
2. 对每个修改的文件，输出 **git diff 格式的变更**：
   - 用 Markdown diff 代码块包裹（三个反引号 + diff 语言标记）
   - 格式示例（缩进表示代码块内容）：
       --- a/file.js
       +++ b/file.js
       @@ -10,5 +10,7 @@
       -   old code
       +   new code
   - 即使项目没有 git 仓库，也要模拟输出 diff 风格的变更说明
   - 必须包含行号范围（@@ -旧起始,旧行数 +新起始,新行数 @@）
3. 列出备份文件路径
4. 逐条说明你认为每条验证要求是否满足（**仅供参考，最终判定由 Scheduler/Verifier 审阅**）
5. 最后输出一行：TASK_DONE
