# 任务 20260604-003

## 角色
你是 Writer（代码修改者）。你只修改代码，不运行测试，不验证结果。

## 项目约束
- 纯前端 HTML/CSS/JS 游戏，单文件结构
- 使用 window.Zhan 命名空间
- data.js 禁止写 function（polyfill 除外）
- core.js 禁止碰 DOM
- ui.js 禁止直接修改 state
- 所有修改必须先备份到指定目录

## 当前任务
修复拆分后 newGame 函数被截断导致卡牌不显示的 bug。newGame 的 G={...}对象定义闭合后，shuffle/buildPiles/updateEnemyIntent/Zhan.UI.render 等后续逻辑错误地落在了函数体外，成为孤儿代码。需要把它们移回 newGame 函数体内。

## 修改范围
code/core.js

## 具体约束
- 只修改 code/core.js，不动其他文件
- newGame 函数必须包含完整逻辑：G 对象定义 + 圣物初始化 + specialCards 处理 + shuffle(G.deck) + buildPiles() + Zhan.Engine.state=G + Zhan.Engine._updateEffectiveFury(G) + updateEnemyIntent() + Zhan.UI.render(G) + log
- shuffle/buildPiles/Zhan.Engine.state/_updateEffectiveFury/updateEnemyIntent/Zhan.UI.render 这些调用必须放在 newGame 函数体内，在 activeRelicNames 赋值之后、函数闭合 } 之前
- 不修改原版 zhan_v1.99_sprint4.html，只改 code/ 下的四个文件
- 改完检查 core.js 括号闭合

## 验证要求
- core.js 括号闭合（{ 和 } 数量相等）
- newGame 函数体内包含 shuffle(G.deck)/buildPiles/Zhan.Engine.state/updateEnemyIntent/Zhan.UI.render 调用
- newGame 函数体外没有残留的 shuffle/buildPiles/Zhan.Engine.state 等调用
- 浏览器打开 code/index.html 卡牌正常显示

## 文件位置
C:\Users\kyzha\.openclaw\workspace\projects\zhan\code\core.js

## 工作目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan

## 备份目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan\msp\archive\backup-20260604-003

## 输出要求（必须逐项完成，缺一不可）
1. 列出你修改的每个文件的**完整绝对路径**
2. 对每个修改的文件，输出 **git diff 格式的变更**：
   - 用 Markdown diff 代码块包裹（三个反引号 + diff 语言标记）
   - 格式示例（缩进表示代码块内容）：
       --- a/file.js
       +++ b/file.js
       @@ -10,5 +10,7 @@
       -   old code
       +   new code
   - 即使项目没有 git 仓库，也要模拟输出 diff 风格的变更说明
   - 必须包含行号范围（@@ -旧起始,旧行数 +新起始,新行数 @@）
3. 列出备份文件路径
4. 逐条说明你认为每条验证要求是否满足（**仅供参考，最终判定由 Scheduler/Verifier 审阅**）
5. 最后输出一行：TASK_DONE
