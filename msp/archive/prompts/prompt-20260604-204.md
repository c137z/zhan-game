# 任务 20260604-204

## 角色
你是 Writer（代码修改者）。你只修改代码，不运行测试，不验证结果。

## 项目约束
- 纯前端 HTML/CSS/JS 游戏，单文件结构
- 使用 window.Zhan 命名空间
- data.js 禁止写 function（polyfill 除外）
- core.js 禁止碰 DOM
- ui.js 禁止直接修改 state
- 所有修改必须先备份到指定目录

## 当前任务
按 config-classification.md 清单批量替换 CONFIG 直读

## 修改范围
code/core.js + code/ui.js

## 具体约束
- 1. CONFIG.MIN_COMBO -> state.effectiveMinCombo || CONFIG.MIN_COMBO (16处)
- 2. CONFIG.ATK_BUFF_MULT -> state.effectiveAtkBuffMult || CONFIG.ATK_BUFF_MULT (3处)
- 3. CONFIG.VULN_MULT -> state.effectiveVulnMult || CONFIG.VULN_MULT (3处)
- 4. CONFIG.ATK_DOWN_PCT -> 读 enemyEffects.atk_down_pct || CONFIG.ATK_DOWN_PCT (4处)
- 5. 真常量不动（BOARD_ROWS/COLS等）
- 6. 参考清单: context/config-classification.md
- 7. 冒烟测试

## 验证要求
- 清单中26处应改全部替换完毕
- 真常量无变动
- 冒烟测试 PASS

## 文件位置
C:\Users\kyzha\.openclaw\workspace\projects\zhan\code\core.js + code\ui.js

## 工作目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan

## 备份目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan\msp\archive\backup-20260604-204

## 输出要求（必须逐项完成，缺一不可）
1. 列出你修改的每个文件的**完整绝对路径**
2. 对每个修改的文件，输出 **git diff 格式的变更**：
   - 用 Markdown diff 代码块包裹（三个反引号 + diff 语言标记）
   - 格式示例（缩进表示代码块内容）：
       --- a/file.js
       +++ b/file.js
       @@ -10,5 +10,7 @@
       -   old code
       +   new code
   - 即使项目没有 git 仓库，也要模拟输出 diff 风格的变更说明
   - 必须包含行号范围（@@ -旧起始,旧行数 +新起始,新行数 @@）
3. 列出备份文件路径
4. 逐条说明你认为每条验证要求是否满足（**仅供参考，最终判定由 Scheduler/Verifier 审阅**）
5. 最后输出一行：TASK_DONE
