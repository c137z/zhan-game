# 任务 20260604-005

## 角色
你是 Writer（代码修改者）。你只修改代码，不运行测试，不验证结果。

## 项目约束
- 纯前端 HTML/CSS/JS 游戏，单文件结构
- 使用 window.Zhan 命名空间
- data.js 禁止写 function（polyfill 除外）
- core.js 禁止碰 DOM
- ui.js 禁止直接修改 state
- 所有修改必须先备份到指定目录

## 当前任务
在 Bridge 中增加 task 完成后的飞书通知功能，替代不可靠的 cron Watcher。

## 修改范围
msp/bridge.js

## 具体约束
- 只改 bridge.js，不动其他文件
- Task 完成后（DONE 或 FAILED），Bridge 拼接一段简洁的通知文本，写入一个 marker 文件 msp/outbox/.notify-{taskId}.txt
- 通知文本格式：'task-{taskId} {DONE|FAILED} — {description}。请 Scheduler 审阅。'
- 如果 status 包含 'FAILED'，通知文本前缀加 '[FAILED]'
- Scheduler（哈基米）通过检查 .notify 文件来知晓 task 完成，而不是依赖不可靠的 cron Watcher
- 不引入新的 npm 依赖
- 现有 bridge.js 的所有功能不受影响

## 验证要求
- bridge.js 仍能正常处理 task（inbox → archive → outbox）
- task 完成后 outbox/.notify-{taskId}.txt 存在
- .notify 文件内容格式正确
- FAILED status 时通知文件包含 [FAILED] 前缀

## 文件位置
C:\Users\kyzha\.openclaw\workspace\projects\zhan\msp\bridge.js

## 工作目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan

## 备份目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan\msp\archive\backup-20260604-005

## 输出要求（必须逐项完成，缺一不可）
1. 列出你修改的每个文件的**完整绝对路径**
2. 对每个修改的文件，输出 **git diff 格式的变更**：
   - 用 Markdown diff 代码块包裹（三个反引号 + diff 语言标记）
   - 格式示例（缩进表示代码块内容）：
       --- a/file.js
       +++ b/file.js
       @@ -10,5 +10,7 @@
       -   old code
       +   new code
   - 即使项目没有 git 仓库，也要模拟输出 diff 风格的变更说明
   - 必须包含行号范围（@@ -旧起始,旧行数 +新起始,新行数 @@）
3. 列出备份文件路径
4. 逐条说明你认为每条验证要求是否满足（**仅供参考，最终判定由 Scheduler/Verifier 审阅**）
5. 最后输出一行：TASK_DONE
