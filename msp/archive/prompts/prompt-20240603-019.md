# 任务 20240603-019

## 角色
你是 Writer（代码修改者）。你只修改代码，不运行测试，不验证结果。

## 项目约束
- 纯前端 HTML/CSS/JS 游戏，单文件结构
- 使用 window.Zhan 命名空间
- data.js 禁止写 function（polyfill 除外）
- core.js 禁止碰 DOM
- ui.js 禁止直接修改 state
- 所有修改必须先备份到指定目录

## 当前任务
统一 enemyTurn 中 hiss/groom 的 hpTrigger 处理风格。当前 hiss 检查用内联 for 循环手动遍历并跳过 groom，groom 检查用 Zhan.Systems.Boss.runHpTriggers(G, 'groom')。两者功能相同但实现风格不一致，且 hiss 遍历中有冗余逻辑（如果 hpTriggers 数组只包含 ['groom', 'hiss']，hiss 的 for 循环实际上只遍历到一个元素）。改为统一使用 runHpTriggers 方法。

## 修改范围
zhan_v1.99_sprint4.html

## 具体约束
- 只改 enemyTurn 函数中 hpTrigger 调用的部分
- 删除内联 for 循环（hiss 检查），改用 Zhan.Systems.Boss.runHpTriggers(G, 'hiss')
- 保持执行顺序不变：hiss 先，然后 groom，中间检查 G.over
- 不改 runHpTriggers 方法本身
- 不改其他逻辑

## 验证要求
- hiss 触发时 game over（playerHP归零）→ groom 不再执行
- hiss 触发但 game 未 over → groom 正常执行
- hiss 未触发（HP 未跨阈值）→ groom 正常按周期执行
- Boss 没有 hiss trigger 时，runHpTriggers(G, 'hiss') 静默跳过（原有行为）

## 文件位置
C:\Users\kyzha\.openclaw\workspace\projects\zhan\zhan_v1.99_sprint4.html

## 工作目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan

## 备份目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan\msp\archive\backup-20240603-019

## 输出要求（必须逐项完成，缺一不可）
1. 列出你修改的每个文件的**完整绝对路径**
2. 对每个修改的文件，输出 **git diff 格式的变更**：
   - 用 Markdown diff 代码块包裹（三个反引号 + diff 语言标记）
   - 格式示例（缩进表示代码块内容）：
       --- a/file.js
       +++ b/file.js
       @@ -10,5 +10,7 @@
       -   old code
       +   new code
   - 即使项目没有 git 仓库，也要模拟输出 diff 风格的变更说明
   - 必须包含行号范围（@@ -旧起始,旧行数 +新起始,新行数 @@）
3. 列出备份文件路径
4. 逐条说明你认为每条验证要求是否满足（**仅供参考，最终判定由 Scheduler/Verifier 审阅**）
5. 最后输出一行：TASK_DONE
