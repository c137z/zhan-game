# 任务 20260604-202c

## 角色
你是 Writer（代码修改者）。你只修改代码，不运行测试，不验证结果。

## 项目约束
- 纯前端 HTML/CSS/JS 游戏，单文件结构
- 使用 window.Zhan 命名空间
- data.js 禁止写 function（polyfill 除外）
- core.js 禁止碰 DOM
- ui.js 禁止直接修改 state
- 所有修改必须先备份到指定目录

## 当前任务
流程控制函数拆分：endGame/updateEnemyIntent/showRelicSelect 的 DOM 操作从 core.js 移到 ui.js

## 修改范围
code/core.js + code/ui.js

## 具体约束
- 1. endGame(win,msg) -> Zhan.Engine._endGame(win,msg)：只做 state 变更（G.over/G.win/统计），DOM 操作移到 ui.js 的 Zhan.UI.showResult(state) 或在统一 render 中处理
- 2. updateEnemyIntent() -> Zhan.Engine._updateEnemyIntent()：设置 intent 状态字段 + Zhan.UI.renderEnemyIntent(state)：DOM 更新 enemy-intent 元素
- 3. showRelicSelect()/renderRelicOptions() -> Zhan.Engine._showRelicSelect() + Zhan.UI.renderRelicSelect(state)
- 4. startNextStage()/startEndlessNextCat() -> Zhan.Engine._startNextStage/_startEndlessNextCat（这两个无 DOM 问题，只改名字）
- 5. core.js 中 document. 引用降到 0
- 6. 冒烟测试 PASS

## 验证要求
- core.js document. 引用 = 0
- 旧全局函数 endGame/updateEnemyIntent/showRelicSelect 不再在 core.js 中以 function xxx() 形式存在
- ui.js 新增 renderEnemyIntent/showResult/renderRelicSelect 中的至少一个
- #test PASS
- 冒烟测试 PASS

## 文件位置
C:\Users\kyzha\.openclaw\workspace\projects\zhan\code\core.js + code\ui.js

## 工作目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan

## 备份目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan\msp\archive\backup-20260604-202c

## 输出要求（必须逐项完成，缺一不可）
1. 列出你修改的每个文件的**完整绝对路径**
2. 对每个修改的文件，输出 **git diff 格式的变更**：
   - 用 Markdown diff 代码块包裹（三个反引号 + diff 语言标记）
   - 格式示例（缩进表示代码块内容）：
       --- a/file.js
       +++ b/file.js
       @@ -10,5 +10,7 @@
       -   old code
       +   new code
   - 即使项目没有 git 仓库，也要模拟输出 diff 风格的变更说明
   - 必须包含行号范围（@@ -旧起始,旧行数 +新起始,新行数 @@）
3. 列出备份文件路径
4. 逐条说明你认为每条验证要求是否满足（**仅供参考，最终判定由 Scheduler/Verifier 审阅**）
5. 最后输出一行：TASK_DONE
