# 任务 20260604-203

## 角色
你是 Writer（代码修改者）。你只修改代码，不运行测试，不验证结果。

## 项目约束
- 纯前端 HTML/CSS/JS 游戏，单文件结构
- 使用 window.Zhan 命名空间
- data.js 禁止写 function（polyfill 除外）
- core.js 禁止碰 DOM
- ui.js 禁止直接修改 state
- 所有修改必须先备份到指定目录

## 当前任务
ui.js 清理：事件处理走 dispatch，不直接裸读 G；logLines 初始化移到 core.js newGame()

## 修改范围
code/ui.js

## 具体约束
- 1. 所有 G. 读取通过 var st = Zhan.Engine.state 获取
- 2. log() 函数中 G.logLines = [] 移到 newGame() 的 G={...logLines:[],...} 初始化
- 3. 事件处理函数确保只发 dispatch action，不直接读 G
- 4. 冒烟测试

## 验证要求
- ui.js 无 G.logLines =
- core.js newGame() 中 G 定义含 logLines: []
- 冒烟测试 PASS

## 文件位置
C:\Users\kyzha\.openclaw\workspace\projects\zhan\code\ui.js

## 工作目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan

## 备份目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan\msp\archive\backup-20260604-203

## 输出要求（必须逐项完成，缺一不可）
1. 列出你修改的每个文件的**完整绝对路径**
2. 对每个修改的文件，输出 **git diff 格式的变更**：
   - 用 Markdown diff 代码块包裹（三个反引号 + diff 语言标记）
   - 格式示例（缩进表示代码块内容）：
       --- a/file.js
       +++ b/file.js
       @@ -10,5 +10,7 @@
       -   old code
       +   new code
   - 即使项目没有 git 仓库，也要模拟输出 diff 风格的变更说明
   - 必须包含行号范围（@@ -旧起始,旧行数 +新起始,新行数 @@）
3. 列出备份文件路径
4. 逐条说明你认为每条验证要求是否满足（**仅供参考，最终判定由 Scheduler/Verifier 审阅**）
5. 最后输出一行：TASK_DONE
