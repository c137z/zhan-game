# 任务 20240603-022

## 角色
你是 Writer（代码修改者）。你只修改代码，不运行测试，不验证结果。

## 项目约束
- 纯前端 HTML/CSS/JS 游戏，单文件结构
- 使用 window.Zhan 命名空间
- data.js 禁止写 function（polyfill 除外）
- core.js 禁止碰 DOM
- ui.js 禁止直接修改 state
- 所有修改必须先备份到指定目录

## 当前任务
消除 updateComboPreview 和 executeTurn 中未消除惩罚计算逻辑的代码重复。两者各有一套 ~30 行的几乎完全相同的逻辑：遍历 slot 中未被 claimed 的卡片，按类型统计 unmatchedByType，然后计算扣血。提取为 Zhan.Rules.computeUnmatchedPenalty(state) 纯函数。

## 修改范围
zhan_v1.99_sprint4.html

## 具体约束
- 新增 Zhan.Rules.computeUnmatchedPenalty(state) 纯函数，接收 state 参数
- 参数 state 包含 slot、_claimedWildIndices、effectiveMinCombo/CONFIG.MIN_COMBO
- 函数返回 { unmatchedByType: {...}, totalUnmatched: number }
- updateComboPreview 中的重复代码改为调用此函数
- executeTurn Phase 3（未消除惩罚）中的重复代码改为调用此函数
- 不改伤害计算逻辑、不改 UI 显示逻辑
- 函数签名不依赖全局 G

## 验证要求
- slot=[attack, heal], minCombo=3 → unmatchedByType={attack:1, heal:1}, totalUnmatched=2
- slot=[attack, attack, attack], minCombo=3 → unmatchedByType={}, totalUnmatched=0
- slot=[attack, wild], minCombo=3（wild 不被 claimed）→ unmatchedByType={attack:1, wild:1}, totalUnmatched=2
- updateComboPreview 和 executeTurn 调用此函数后结果一致

## 文件位置
C:\Users\kyzha\.openclaw\workspace\projects\zhan\zhan_v1.99_sprint4.html

## 工作目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan

## 备份目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan\msp\archive\backup-20240603-022

## 输出要求（必须逐项完成，缺一不可）
1. 列出你修改的每个文件的**完整绝对路径**
2. 对每个修改的文件，输出 **git diff 格式的变更**：
   - 用 Markdown diff 代码块包裹（三个反引号 + diff 语言标记）
   - 格式示例（缩进表示代码块内容）：
       --- a/file.js
       +++ b/file.js
       @@ -10,5 +10,7 @@
       -   old code
       +   new code
   - 即使项目没有 git 仓库，也要模拟输出 diff 风格的变更说明
   - 必须包含行号范围（@@ -旧起始,旧行数 +新起始,新行数 @@）
3. 列出备份文件路径
4. 逐条说明你认为每条验证要求是否满足（**仅供参考，最终判定由 Scheduler/Verifier 审阅**）
5. 最后输出一行：TASK_DONE
