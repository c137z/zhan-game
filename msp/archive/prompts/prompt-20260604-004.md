# 任务 20260604-004

## 角色
你是 Writer（代码修改者）。你只修改代码，不运行测试，不验证结果。

## 项目约束
- 纯前端 HTML/CSS/JS 游戏，单文件结构
- 使用 window.Zhan 命名空间
- data.js 禁止写 function（polyfill 除外）
- core.js 禁止碰 DOM
- ui.js 禁止直接修改 state
- 所有修改必须先备份到指定目录

## 当前任务
修复拆分后卡牌不显示的 bug。根因：newGame() 在 core.js 末尾执行时，ui.js 尚未加载，Zhan.UI 未定义，render 被跳过。修复：从 core.js 中移除 newGame() 启动调用，改为在 index.html 中 ui.js 之后加一个内联 script 标签调用 newGame()。

## 修改范围
code/core.js + code/ui.js + code/index.html

## 具体约束
- 从 core.js 末尾移除启动脚本块（G.currentStage=1; G.bossId='skeleton'; G.isEndless=false; ENDLESS_DEFEATED={}; newGame(); 这几行）
- 在 index.html 中 <script src='ui.js'></script> 之后添加一个内联 <script> 标签，内容为启动脚本 + Test 脚本
- 不修改任何函数逻辑，只移动启动脚本的执行位置
- 确保 Zhan.Test 代码也跟着移到 index.html 内联 script 中
- 最后一个 </script> 后直接 </body></html> 收尾

## 验证要求
- core.js 末尾不再有 G.currentStage/G.bossId/newGame() 等启动代码
- index.html 中 ui.js 之后有一个内联 <script> 标签包含启动脚本 + Test
- 浏览器打开 index.html 卡牌正常显示
- 浏览器打开 index.html#test 回归测试通过
- 所有文件括号闭合

## 文件位置
C:\Users\kyzha\.openclaw\workspace\projects\zhan\code\core.js + code\ui.js + code\index.html

## 工作目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan

## 备份目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan\msp\archive\backup-20260604-004

## 输出要求（必须逐项完成，缺一不可）
1. 列出你修改的每个文件的**完整绝对路径**
2. 对每个修改的文件，输出 **git diff 格式的变更**：
   - 用 Markdown diff 代码块包裹（三个反引号 + diff 语言标记）
   - 格式示例（缩进表示代码块内容）：
       --- a/file.js
       +++ b/file.js
       @@ -10,5 +10,7 @@
       -   old code
       +   new code
   - 即使项目没有 git 仓库，也要模拟输出 diff 风格的变更说明
   - 必须包含行号范围（@@ -旧起始,旧行数 +新起始,新行数 @@）
3. 列出备份文件路径
4. 逐条说明你认为每条验证要求是否满足（**仅供参考，最终判定由 Scheduler/Verifier 审阅**）
5. 最后输出一行：TASK_DONE
