# 任务 20260604-301

## 角色
你是 Writer（代码修改者）。你只修改代码，不运行测试，不验证结果。

## 项目约束
- 纯前端 HTML/CSS/JS 游戏，单文件结构
- 使用 window.Zhan 命名空间
- data.js 禁止写 function（polyfill 除外）
- core.js 禁止碰 DOM
- ui.js 禁止直接修改 state
- 所有修改必须先备份到指定目录

## 当前任务
修复 BUG-4：Fury 默认值导致攻击无条件翻倍。computeEffectiveFury() 在 furyEnabled=false 时把 atkBuffMult=1.5 和 vulnMult=1.5 作为默认值返回，导致新局所有攻击×2.25。

## 修改范围
code/core.js

## 具体约束
- 1. 修改 Zhan.Rules.computeEffectiveFury(playerHP, playerMaxHP, baseValues) — 这是纯函数，无 DOM 依赖
- 2. atkBuffMult 默认值改为 1.0（当 furyEnabled=false 或 atkBuffMult 未传入时，不翻转）
- 3. vulnMult 默认值改为 1.0（无易伤时不翻转）
- 4. fury 倍率只作用于 buff/debuff 激活时的额外部分：effective = 1 + (base - 1) * furyMult
- 5. 检查 Zhan.Engine._updateEffectiveFury 中的调用点，确认 baseValues 传入时的 fallback 逻辑正确
- 6. 确认 defBuffRatio 的计算逻辑是否也有同样问题（检查减伤是否也是无条件生效）
- 7. 不修改其他 game 逻辑
- 8. 冒烟测试：新局第一回合 3 连攻击打不出 9 血

## 验证要求
- computeEffectiveFury 在 furyEnabled=false 时 atkBuffMult 为 1.0
- computeEffectiveFury 在 furyEnabled=false 时 vulnMult 为 1.0
- fury 倍率公式：effective = 1 + (base-1) * furyMult（不对 1.0 产生多余放大）
- 新局无 buff 无 debuff 时，攻击伤害正常（不被放大到 ×2.25）
- #test PASS
- 冒烟测试 PASS

## 文件位置
C:\Users\kyzha\.openclaw\projects\zhan\code\core.js

## 工作目录
C:\Users\kyzha\.openclaw\projects\zhan

## 备份目录
C:\Users\kyzha\.openclaw\projects\zhan\msp\archive\backup-20260604-301

## 输出要求（必须逐项完成，缺一不可）
1. 列出你修改的每个文件的**完整绝对路径**
2. 对每个修改的文件，输出 **git diff 格式的变更**：
   - 用 Markdown diff 代码块包裹（三个反引号 + diff 语言标记）
   - 格式示例（缩进表示代码块内容）：
       --- a/file.js
       +++ b/file.js
       @@ -10,5 +10,7 @@
       -   old code
       +   new code
   - 即使项目没有 git 仓库，也要模拟输出 diff 风格的变更说明
   - 必须包含行号范围（@@ -旧起始,旧行数 +新起始,新行数 @@）
3. 列出备份文件路径
4. 逐条说明你认为每条验证要求是否满足（**仅供参考，最终判定由 Scheduler/Verifier 审阅**）
5. 最后输出一行：TASK_DONE
