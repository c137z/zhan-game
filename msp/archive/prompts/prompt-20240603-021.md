# 任务 20240603-021

## 角色
你是 Writer（代码修改者）。你只修改代码，不运行测试，不验证结果。

## 项目约束
- 纯前端 HTML/CSS/JS 游戏，单文件结构
- 使用 window.Zhan 命名空间
- data.js 禁止写 function（polyfill 除外）
- core.js 禁止碰 DOM
- ui.js 禁止直接修改 state
- 所有修改必须先备份到指定目录

## 当前任务
去除 dispatch END_TURN 后的双重渲染。当前 Zhan.Engine.dispatch 在处理 END_TURN action 时调用 executeTurn()（内部有多处 render 调用），然后 dispatch 末尾又无条件调用 Zhan.UI.render。导致一次 END_TURN 至少 2 次完整渲染。修复：在 dispatch 的 render 调用前判断 action.type 是否为 END_TURN（executeTurn 内部已处理 render），或是让 executeTurn 内部不再显式调用 render 而全权交给 dispatch。

## 修改范围
zhan_v1.99_sprint4.html

## 具体约束
- 只改 Zhan.Engine.dispatch 函数
- 方案：dispatch 末尾的 render 调用处添加判断——如果 action.type === 'END_TURN' 则跳过 render（executeTurn 内部已负责渲染）
- 其他 action（PLAY_CARD、RESET、RESTART、START_ENDLESS）保持原有的 dispatch 末尾 render 行为
- 不改 executeTurn 内部的 render 调用
- 不改 UI 渲染逻辑

## 验证要求
- dispatch END_TURN → executeTurn 渲染一次，dispatch 末尾不再渲染第二次
- dispatch PLAY_CARD → pullCard 不自己渲染，dispatch 末尾渲染一次
- dispatch RESET → newGame + dispatch 末尾渲染一次
- dispatch RESTART → newGame + dispatch 末尾渲染一次

## 文件位置
C:\Users\kyzha\.openclaw\workspace\projects\zhan\zhan_v1.99_sprint4.html

## 工作目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan

## 备份目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan\msp\archive\backup-20240603-021

## 输出要求（必须逐项完成，缺一不可）
1. 列出你修改的每个文件的**完整绝对路径**
2. 对每个修改的文件，输出 **git diff 格式的变更**：
   - 用 Markdown diff 代码块包裹（三个反引号 + diff 语言标记）
   - 格式示例（缩进表示代码块内容）：
       --- a/file.js
       +++ b/file.js
       @@ -10,5 +10,7 @@
       -   old code
       +   new code
   - 即使项目没有 git 仓库，也要模拟输出 diff 风格的变更说明
   - 必须包含行号范围（@@ -旧起始,旧行数 +新起始,新行数 @@）
3. 列出备份文件路径
4. 逐条说明你认为每条验证要求是否满足（**仅供参考，最终判定由 Scheduler/Verifier 审阅**）
5. 最后输出一行：TASK_DONE
