# 任务 20240603-018

## 角色
你是 Writer（代码修改者）。你只修改代码，不运行测试，不验证结果。

## 项目约束
- 纯前端 HTML/CSS/JS 游戏，单文件结构
- 使用 window.Zhan 命名空间
- data.js 禁止写 function（polyfill 除外）
- core.js 禁止碰 DOM
- ui.js 禁止直接修改 state
- 所有修改必须先备份到指定目录

## 当前任务
消除两处全局 G 依赖反模式：1）移除 6 个函数中的 `|| window.G` fallback（showRelicSelect/renderRelicOptions/startNextStage 等），改为只用 `Zhan.Engine.state`，state 为 null 时 return 或 skip 而非静默回退旧 state。2）`getEffectDescription` 改为接收 state 参数，将 `_updateEffectiveFury(G)` 副作用移到调用方。

## 修改范围
zhan_v1.99_sprint4.html

## 具体约束
- 搜索所有 `var G = Zhan.Engine.state || window.G` 出现位置，移除 `|| window.G`，改为 `var G = Zhan.Engine.state`
- 如果 G 为 null/falsy，添加 guard：`if (!G) return;`
- 搜索所有 `|| window.G` 模式，确保全部清理
- getEffectDescription(type, n) 改为 getEffectDescription(state, type, n)，内部全部改用 state 而非全局 G
- 将 getEffectDescription 内的 Zhan.Engine._updateEffectiveFury(state) 调用移除，改为在调用方先调用 _updateEffectiveFury 再传 state
- 不改其他逻辑

## 验证要求
- showRelicSelect 在 Engine.state 为 null 时不操作 window.G（旧行为：静默回退）
- getEffectDescription 传入 mock state，返回正确描述，不读写全局 G
- 所有调用 getEffectDescription 的地方，先确保 _updateEffectiveFury 已调用，再传入 state
- getEffectDescription 不再有副作用（不调用 _updateEffectiveFury，不修改任何 G 字段）

## 文件位置
C:\Users\kyzha\.openclaw\workspace\projects\zhan\zhan_v1.99_sprint4.html

## 工作目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan

## 备份目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan\msp\archive\backup-20240603-018

## 输出要求（必须逐项完成，缺一不可）
1. 列出你修改的每个文件的**完整绝对路径**
2. 对每个修改的文件，输出 **git diff 格式的变更**：
   - 用 Markdown diff 代码块包裹（三个反引号 + diff 语言标记）
   - 格式示例（缩进表示代码块内容）：
       --- a/file.js
       +++ b/file.js
       @@ -10,5 +10,7 @@
       -   old code
       +   new code
   - 即使项目没有 git 仓库，也要模拟输出 diff 风格的变更说明
   - 必须包含行号范围（@@ -旧起始,旧行数 +新起始,新行数 @@）
3. 列出备份文件路径
4. 逐条说明你认为每条验证要求是否满足（**仅供参考，最终判定由 Scheduler/Verifier 审阅**）
5. 最后输出一行：TASK_DONE
