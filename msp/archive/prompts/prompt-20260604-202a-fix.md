# 任务 20260604-202a-fix

## 角色
你是 Writer（代码修改者）。你只修改代码，不运行测试，不验证结果。

## 项目约束
- 纯前端 HTML/CSS/JS 游戏，单文件结构
- 使用 window.Zhan 命名空间
- data.js 禁止写 function（polyfill 除外）
- core.js 禁止碰 DOM
- ui.js 禁止直接修改 state
- 所有修改必须先备份到指定目录

## 当前任务
修复 202a 遗漏：Zhan.Rules 对象中缺少 getEffectDescription 方法定义。原函数已从全局删除，但未挂到 Zhan.Rules 上。从备份或原版 v1.99 中找回函数体，加入 Zhan.Rules 对象。

## 修改范围
code/core.js

## 具体约束
- 1. 从 C:\Users\kyzha\.openclaw\workspace\projects\zhan\msp\archive\backup-20260604-202a\core.js 中找到原 getEffectDescription 函数体
- 2. 挂到 Zhan.Rules 对象内：Zhan.Rules.getEffectDescription = function(state, type, n) { ... }
- 3. 确保 ui.js 的 Zhan.Rules.getEffectDescription 调用能找到这个方法
- 4. 不修改其他代码

## 验证要求
- core.js 中 Zhan.Rules.getEffectDescription 存在
- core.js 中不存在全局 function getEffectDescription
- #test PASS
- 冒烟测试 PASS

## 文件位置
C:\Users\kyzha\.openclaw\workspace\projects\zhan\code\core.js

## 工作目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan

## 备份目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan\msp\archive\backup-20260604-202a-fix

## 输出要求（必须逐项完成，缺一不可）
1. 列出你修改的每个文件的**完整绝对路径**
2. 对每个修改的文件，输出 **git diff 格式的变更**：
   - 用 Markdown diff 代码块包裹（三个反引号 + diff 语言标记）
   - 格式示例（缩进表示代码块内容）：
       --- a/file.js
       +++ b/file.js
       @@ -10,5 +10,7 @@
       -   old code
       +   new code
   - 即使项目没有 git 仓库，也要模拟输出 diff 风格的变更说明
   - 必须包含行号范围（@@ -旧起始,旧行数 +新起始,新行数 @@）
3. 列出备份文件路径
4. 逐条说明你认为每条验证要求是否满足（**仅供参考，最终判定由 Scheduler/Verifier 审阅**）
5. 最后输出一行：TASK_DONE
