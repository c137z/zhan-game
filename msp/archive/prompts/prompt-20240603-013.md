# 任务 20240603-013

## 角色
你是 Writer（代码修改者）。你只修改代码，不运行测试，不验证结果。

## 项目约束
- 纯前端 HTML/CSS/JS 游戏，单文件结构
- 使用 window.Zhan 命名空间
- data.js 禁止写 function（polyfill 除外）
- core.js 禁止碰 DOM
- ui.js 禁止直接修改 state
- 所有修改必须先备份到指定目录

## 当前任务
修复坚韧核心重复触发：applyDamageToPlayer 末尾和 executeTurn Phase 3 末尾都有相同的 tenacity 检查逻辑（if tenacityUsed===false && playerHP<=0: playerHP=1, tenacityUsed=true, log）。提取为 checkTenacity(state) 函数，在 applyDamageToPlayer 末尾和 executeTurn Phase 3 末尾各调用一次。注意：applyDamageToPlayer 里的触发是在 Boss 伤害结算后，executeTurn 里的触发是在未消除惩罚结算后。两个场景都需要触发，但逻辑必须只写一次。

## 修改范围
zhan_v1.99_sprint4.html

## 具体约束
- 不改UI
- 保持Zhan命名空间
- 不删CSS
- 提取为纯函数 checkTenacity(state)，接收 state 对象，修改 state.playerHP 和 state.tenacityUsed
- 原两处重复代码替换为 checkTenacity(G) 调用

## 验证要求
- Boss攻击把玩家HP从2打到0，applyDamageToPlayer触发tenacity，HP=1，tenacityUsed=true
- 未消除惩罚把玩家HP从2打到0，executeTurn Phase3触发tenacity，HP=1，tenacityUsed=true
- 两个场景都只触发一次，不会连续触发两次
- tenacityUsed=true后再次受到致命伤害，不再触发，正常判负

## 文件位置
C:\Users\kyzha\.openclaw\workspace\projects\zhan\zhan_v1.99_sprint4.html

## 工作目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan

## 备份目录
C:\Users\kyzha\.openclaw\workspace\projects\zhan\msp\archive\backup-20240603-013

## 输出要求（必须逐项完成，缺一不可）
1. 列出你修改的每个文件的**完整绝对路径**
2. 对每个修改的文件，输出 **git diff 格式的变更**：
   - 用 Markdown diff 代码块包裹（三个反引号 + diff 语言标记）
   - 格式示例（缩进表示代码块内容）：
       --- a/file.js
       +++ b/file.js
       @@ -10,5 +10,7 @@
       -   old code
       +   new code
   - 即使项目没有 git 仓库，也要模拟输出 diff 风格的变更说明
   - 必须包含行号范围（@@ -旧起始,旧行数 +新起始,新行数 @@）
3. 列出备份文件路径
4. 逐条说明你认为每条验证要求是否满足（**仅供参考，最终判定由 Scheduler/Verifier 审阅**）
5. 最后输出一行：TASK_DONE
