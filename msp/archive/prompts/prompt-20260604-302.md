# 任务 20260604-302

## 角色
你是 Writer（代码修改者）。你只修改代码，不运行测试，不验证结果。

## 项目约束
- 纯前端 HTML/CSS/JS 游戏，单文件结构
- 使用 window.Zhan 命名空间
- data.js 禁止写 function（polyfill 除外）
- core.js 禁止碰 DOM
- ui.js 禁止直接修改 state
- 所有修改必须先备份到指定目录

## 当前任务
修复 BUG-1：Preview UI 显示旧结算值（读上一回合残留的 enemyEffects / effective 值），与实际结算不一致

## 修改范围
code/core.js + code/ui.js

## 具体约束
- 1. getEffectDescription 的 preview 调用在 ui.js updateComboPreview 中，需要让它在计算时使用即将生效的值而非当前 state 的旧值
- 2. 降攻：preview 应直接算 (ee.atk_down || 0) + 1 回合后的值，百分比用 ee.atk_down_pct || CONFIG.ATK_DOWN_PCT（不变，不受旧 effective 影响）
- 3. 护盾/眩晕同理：preview 应基于当前 state 类型 + 即将生效的 combo 数来计算新的 buff 层数/回合数
- 4. 修改范围：Zhan.Rules.getEffectDescription 和 ui.js Zhan.UI.updateComboPreview 调用逻辑
- 5. 不修改其他逻辑
- 6. 冒烟测试 PASS

## 验证要求
- preview 降攻百分比不受上一回合旧 fury 值影响
- preview 护盾显示与实际结算一致
- preview 眩晕回合数与实际结算一致
- #test PASS
- 冒烟测试 PASS

## 文件位置
C:\Users\kyzha\.openclaw\projects\zhan\code\core.js + code\ui.js

## 工作目录
C:\Users\kyzha\.openclaw\projects\zhan

## 备份目录
C:\Users\kyzha\.openclaw\projects\zhan\msp\archive\backup-20260604-302

## 输出要求（必须逐项完成，缺一不可）
1. 列出你修改的每个文件的**完整绝对路径**
2. 对每个修改的文件，输出 **git diff 格式的变更**：
   - 用 Markdown diff 代码块包裹（三个反引号 + diff 语言标记）
   - 格式示例（缩进表示代码块内容）：
       --- a/file.js
       +++ b/file.js
       @@ -10,5 +10,7 @@
       -   old code
       +   new code
   - 即使项目没有 git 仓库，也要模拟输出 diff 风格的变更说明
   - 必须包含行号范围（@@ -旧起始,旧行数 +新起始,新行数 @@）
3. 列出备份文件路径
4. 逐条说明你认为每条验证要求是否满足（**仅供参考，最终判定由 Scheduler/Verifier 审阅**）
5. 最后输出一行：TASK_DONE
